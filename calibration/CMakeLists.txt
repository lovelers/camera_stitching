FIND_PACKAGE( OpenCV REQUIRED )

ADD_DEFINITIONS(-DOPENCV)

INCLUDE_DIRECTORIES(${OpenCV_INCLUDE_DIRS})

ADD_EXECUTABLE(${CALIBRATION_NAME} "main.cpp"  "stitching_calibration.cpp" )

TARGET_LINK_LIBRARIES(${CALIBRATION_NAME} ${OpenCV_LIBS} )
